///scr_compare_3d_points( x1, y1, z1, x2, y2, z2 )
//
//Returns if (x1,y1,z1) and (x2,y2,z2) are the same coordinates
//This makes many things in this engine much neater to read

if ( argument0 == argument3 ) and ( argument1 == argument4 ) and ( argument2 == argument5 ) return true else return false;
