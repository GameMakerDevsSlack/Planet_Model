///destroy_game_objects()
//Destroys all the interactive elements that the game makes

with( obj_faction ) instance_destroy();
with( obj_army ) instance_destroy();
with( obj_region ) instance_destroy();
