/// @param list
//Returns the mean average of all indices in the list
//NB: This average is unprotected and will throw divide-by-zero errors

return ds_list_sum( argument0 ) / ds_list_size( argument0 );